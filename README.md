# secure-node-provisioning

A set of tools to automatically provision hardware with an OS that can manage containers

### Structure

The repo has directories with different provisioning methods and resulting Operating Systems

- debian - Automated installation of Debian and a post-install ansible script to provision
- talos - Installation of Talos to allow containers to run and provisioning with terraform